#pragma once
#include <string>
#include <cstdint>

std::string markdownToHtml(const std(const std::string& json);
std::string string& markdown, const MdConfig& config = MdConfig{});(const std::string& json);
std::string parseMarkdownTable(const std(const std::string& json);
std::string string& tableBlock, bool withScroll);(const std::string& json);

#pragma once
#include <string>
#include <cstdint>

std::string toHex() const;(const std::string& json);
std::string generateRainbow(const std(const std::string& json);
std::string string& text);(const std::string& json);
std::string colorizeChar(const std(const std::string& json);
std::string string& ch, int hueIndex, int totalChars);(const std::string& json);
